<?xml version="1.0" encoding="utf-8"?>
<s:ViewNavigatorApplication xmlns:fx="http://ns.adobe.com/mxml/2009"
			   xmlns:s="library://ns.adobe.com/flex/spark"
			   applicationDPI="160" creationComplete="buildcomplite(event)"
			   splashScreenImage="@Embed('img/splashScreen.png')"
			   splashScreenScaleMode="letterbox"
			   firstView="views.PswMenu">
	<fx:Declarations>
		<!-- Place non-visual elements (e.g., services, value objects) here -->
	</fx:Declarations>
	<fx:Script>
		<![CDATA[
			import mx.events.FlexEvent;
			
			import spark.components.NavigatorContent;
			
			protected function buildcomplite(event:FlexEvent):void
			{
				NativeApplication.nativeApplication.addEventListener(Event.DEACTIVATE, apliInactiva);
			}
			
			protected function apliInactiva(event:Event):void
			{
				//NativeApplication.nativeApplication.exit();
			}
			
			protected function button1_clickHandler(event:MouseEvent):void
			{
				
			}
			
		]]>
	</fx:Script>
	
</s:ViewNavigatorApplication>
